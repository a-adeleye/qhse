import { Component } from '@angular/core';
import {RouterLink} from '@angular/router';

@Component({
  selector: 'app-sync-banner',
  templateUrl: './sync-banner.html',
  imports: [
    RouterLink
  ],
  styleUrls: ['./sync-banner.scss']
})
export class SyncBanner {
}
